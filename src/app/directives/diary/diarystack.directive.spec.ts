import { DiarystackDirective } from './diarystack.directive';

describe('DiarystackDirective', () => {
  it('should create an instance', () => {
    const directive = new DiarystackDirective();
    expect(directive).toBeTruthy();
  });
});
