import { DiarycardDirective } from './diarycard.directive';

describe('DiarycardDirective', () => {
  it('should create an instance', () => {
    const directive = new DiarycardDirective();
    expect(directive).toBeTruthy();
  });
});
