import { DiaryentryDirective } from './diaryentry.directive';

describe('DiaryentryDirective', () => {
  it('should create an instance', () => {
    const directive = new DiaryentryDirective();
    expect(directive).toBeTruthy();
  });
});
