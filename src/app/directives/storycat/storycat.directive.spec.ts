import { StorycatDirective } from './storycat.directive';

describe('StorycatDirective', () => {
  it('should create an instance', () => {
    const directive = new StorycatDirective();
    expect(directive).toBeTruthy();
  });
});
